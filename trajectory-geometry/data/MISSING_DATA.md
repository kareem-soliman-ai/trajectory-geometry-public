# Missing Data

No required named source CSVs from the public-repo setup prompt were missing in the local
workspace when this export was assembled.

Notes:

- The `qwen15b/` and `pythia410m/` folders contain selected replication tables rather than
  raw hidden-state dumps.
- Large model weight files and intermediate `.npy` hidden-state archives are intentionally
  excluded from this public-facing export.
